-
    path: event_type
    value: floatingip.update.end
    cmd: /tmp/float.sh
    args:
        - _context_tenant_id
        - payload.floatingip.floating_ip_address
        - payload.floatingip.port_id
